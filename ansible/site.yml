---
- hosts: utmp
  # gather_facts: yes
  become: yes
  roles:
    - {role: prepare, tags: 'prepare'}
    - {role: swag, tags: 'swag'}
    - {role: registry, tags: 'registry'}
    - {role: drone, tags: 'drone'}